# config.py

# Initial points generation params
initial_points_num = 20
lower_bound = -5000
upper_bound = 5000

# Additional points generation params
additional_points_num = 20000
additional_points_num_medoid = 10000
offset_range = 100

# Clusterization params
clusterization_method = "unify_clustering"      # centroid_clustering, medoid_clustering, unify_clustering
max_distance = 800

# Evaluation params
max_average_distance = 500

